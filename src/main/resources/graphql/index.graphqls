type User {
    id: ID!
    name: String!
    email: String!
    description: String
    createdAt: String
}

type Query {
    getUsers: [User!]
    getUser(id: ID) : User
}

type Mutation {
    createUser( name: String!, email: String!, description: String) : User!
}